= render 'nodes', content_type: "dépêche"

=h1 "#{@user.name} a écrit #{pluralize @nodes.total_count, "dépêche"}"
- feed "Flux Atom des dépêches de #{@user.name}"
- filter_pagination_params ['id', 'order', 'page']
= paginated_nodes @nodes
