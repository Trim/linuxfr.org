= render 'nodes', content_type: "lien"

=h1 "#{@user.name} a partagé #{pluralize @nodes.total_count, "lien"}"
- feed "Flux Atom des liens de #{@user.name}"
- filter_pagination_params ['id', 'order', 'page']
= paginated_nodes @nodes
