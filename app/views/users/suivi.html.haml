= render 'nodes', content_type: "entrée de suivi"

=h1 "#{@user.name} a écrit #{pluralize @nodes.total_count, "entrée"} sur le suivi"
- filter_pagination_params ['id', 'order', 'page']
= paginated_nodes @nodes
