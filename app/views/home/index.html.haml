%main
	%section
		%h1
			= link_to "Actualités", '/news', title: "Toutes les actualités"
		= render :partial => 'news/short', :collection => @news.map(&:content), :as => :news
		= link_to "Plus d'actualités", '/news', title: "Toutes les actualités", :class => "button center vertical-margin"
	%section#banner
		= @banner
	%section
		%h1
			= link_to "Derniers billets de la communauté", '/journaux', title: "Tous les billets de la communauté"
		= render :partial => 'diaries/short', :collection => @diaries.map(&:content), :as => :diary
		= link_to "Plus de billets", '/journaux', title: "Tous les billets de la communauté", :class => "button center vertical-margin"
	%section
		= render :partial => 'incitation'

%aside
	%section
		%h1
			= link_to "Sur le forum", '/forums', title: "Questions‑réponses, petites annonces"
		%nav
			= render :partial => 'posts/short', :collection => @posts.map(&:content), :as => :post
	%section#polls
		%h1
			= link_to "Sondage", '/sondages', title: "Sondage"
		= render :partial => 'polls/short', :collection => @polls.map(&:content), :as => :poll
	%section
		%h1
			= link_to "Liens remarquables", '/liens', title: "Liens remarquables"
		%nav
			= render :partial => 'bookmarks/short', :collection => @bookmarks.map(&:content), :as => :bookmark
	%section
		%h1
			= link_to "Sur le wiki", '/wiki', title: "Pages wiki"
		%nav
			= render :partial => 'wiki_pages/short', :collection => @wiki_pages.map(&:content), :as => :wiki_page
